# Быстрый старт - Система обмена данными

## 🚀 Запуск за 5 минут

### 1. Предварительные требования
- .NET 8.0 SDK
- PostgreSQL 12+
- Git

### 2. Установка и настройка

```bash
# Клонируйте проект (если нужно)
git clone <repository-url>
cd ExchangeSystem

# Запустите автоматическую настройку
./setup.sh
```

### 3. Запуск приложения

```bash
# Запустите приложение
./start.sh
```

### 4. Вход в систему

Откройте браузер: `https://localhost:5001`

**Тестовые данные:**
- Имя пользователя: `admin`
- Пароль: `admin123`

## 📁 Импорт данных

### CSV файлы
1. Перейдите в раздел "Импорт данных"
2. Выберите CSV файл с разделителем `;` и кодировкой Windows-1251
3. Нажмите "Импортировать CSV"

### DBF файлы
1. Выберите DBF файл в формате dBASE IV
2. Нажмите "Импортировать DBF"

## 🔗 Объединение данных

1. Перейдите в "Панель управления"
2. Нажмите "Объединить данные"
3. Система автоматически свяжет записи и сгенерирует ключи

## 📊 Управление данными

- **Продукты**: Добавление, редактирование, удаление продуктов
- **Магазины**: Управление магазинами и их данными
- **Транзакции**: Просмотр и управление транзакциями
- **Отчеты**: Создание отчетов и экспорт данных

## 🛠️ Разработка

### Структура проекта
```
ExchangeSystem/
├── Controllers/     # API контроллеры
├── Models/         # Модели данных
├── Services/       # Бизнес-логика
├── Data/          # Контекст БД
├── Views/         # Razor представления
├── wwwroot/       # Статические файлы
└── Program.cs     # Точка входа
```

### Полезные команды
```bash
# Создание миграции
dotnet ef migrations add MigrationName

# Применение миграций
dotnet ef database update

# Запуск в режиме разработки
dotnet run --environment Development

# Сборка для продакшена
dotnet publish -c Release
```

## 🔧 Конфигурация

### База данных
Отредактируйте `appsettings.json`:
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Database=ExchangeSystem;Username=postgres;Password=your_password"
  }
}
```

### JWT настройки
```json
{
  "Jwt": {
    "Key": "YourSuperSecretKeyThatIsAtLeast32CharactersLong!",
    "Issuer": "ExchangeSystem",
    "Audience": "ExchangeSystemUsers"
  }
}
```

## 📋 API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/validate` - Валидация токена

### Импорт
- `POST /api/import/csv` - Импорт CSV
- `POST /api/import/dbf` - Импорт DBF

### Данные
- `GET /api/data/joined` - Объединенные данные
- `GET /api/data/products` - Продукты
- `GET /api/data/stores` - Магазины
- `GET /api/data/transactions` - Транзакции

## 🆘 Поддержка

### Частые проблемы

**Ошибка подключения к БД:**
- Проверьте, что PostgreSQL запущен
- Убедитесь в правильности строки подключения

**Ошибка импорта файлов:**
- Проверьте кодировку CSV файлов (должна быть Windows-1251)
- Убедитесь в правильности разделителей

**Проблемы с аутентификацией:**
- Проверьте JWT настройки в appsettings.json
- Убедитесь, что токен не истек

### Логи
Логи приложения выводятся в консоль. Для детальной диагностики включите логирование в `appsettings.Development.json`.

## 📞 Контакты

Для получения поддержки создайте issue в репозитории или обратитесь к команде разработки.

---

*Версия: 1.0.0 | Дата: 2025-01-03*



